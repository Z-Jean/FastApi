# 生产环境：复制为 .env.production 后填入真实域名或 IP
# cp .env.production.example .env.production

# 有域名且已配置 HTTPS（推荐）
NEXT_PUBLIC_API_URL=https://你的域名.com

# 仅用公网 IP 测试（无 HTTPS）时使用下面一行并注释上面一行
# NEXT_PUBLIC_API_URL=http://你的公网IP
